name: CI

on:
  push:
    branches: [yolo]
  pull_request:
    branches: [yolo]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-15
            name: macOS ARM
          - os: ubuntu-latest
            name: Linux Intel
          - os: ubuntu-24.04-arm
            name: Linux ARM
          - os: windows-latest
            name: Windows Intel

    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6

      - name: Install Zig

        shell: bash

        run: |

          set -euo pipefail

          version="0.16.0"

          if [ "$version" = "latest" ]; then

            version="$(curl -fsSL https://ziglang.org/download/index.json | ruby -rjson -e 'data = JSON.parse(STDIN.read); puts data.keys.grep(/\A\d+\.\d+\.\d+\z/).sort_by { |v| v.split(".").map(&:to_i) }.last')"

          fi

          case "${RUNNER_OS}-${RUNNER_ARCH}" in

            Linux-X64) platform="x86_64-linux"; ext="tar.xz" ;;

            Linux-ARM64) platform="aarch64-linux"; ext="tar.xz" ;;

            macOS-X64) platform="x86_64-macos"; ext="tar.xz" ;;

            macOS-ARM64) platform="aarch64-macos"; ext="tar.xz" ;;

            Windows-X64) platform="x86_64-windows"; ext="zip" ;;

            *) echo "Unsupported runner: ${RUNNER_OS}-${RUNNER_ARCH}" >&2; exit 1 ;;

          esac

          archive="$RUNNER_TEMP/zig.$ext"

          install_dir="$RUNNER_TEMP/zig"

          curl -fsSL "https://ziglang.org/download/${version}/zig-${platform}-${version}.${ext}" -o "$archive"

          mkdir -p "$install_dir"

          if [ "$ext" = "zip" ]; then

            unzip -q "$archive" -d "$install_dir"

          else

            tar -xf "$archive" -C "$install_dir"

          fi

          zig_dir="$(find "$install_dir" -mindepth 1 -maxdepth 1 -type d | head -n 1)"

          echo "$zig_dir" >> "$GITHUB_PATH"

          "$zig_dir/zig" version

      - name: Build
        run: zig build

      - name: Test
        run: zig build test

  nix:
    name: Nix flake checks (x86_64-linux, sandboxed)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: DeterminateSystems/nix-installer-action@main
      - name: nix build flake checks (replaces Garnix; shutting down 2026-07-15)
        run: nix build '.#checks.x86_64-linux.default' -L
      - name: nix build flake package
        run: nix build '.#packages.x86_64-linux.default' -L
